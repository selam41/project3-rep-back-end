name: DEPLOY TO STAGING

on:
    pull_request:
        types: [closed]
        branches:
            - dev

jobs:
    aws-deploy:
        runs-on: ubuntu-latest
        env:
          DB_HOST: ${{ secrets.AWS_DB_HOST }}
          DB_PW: ${{ secrets.AWS_DB_PW}}
          DB_USER: ${{ secrets.AWS_DB_USER }}
          DB_NAME: ${{ secrets.AWS_DB_NAME }}
          CLIENT_ID : ${{secrets.CLIENT_ID}}
          CLIENT_SECRET : ${{secrets.CLIENT_SECRET}}
        steps:
            - uses: actions/checkout@v2
              with:
                  ref: dev
            - uses: actions/setup-node@v1
            - run: npm install
            - run: npm run bundle
            - name: Deploy to Server
              uses: easingthemes/ssh-deploy@v2.1.1
              env:
                  SSH_PRIVATE_KEY: ${{ secrets.SERVER_SSH_KEY }}
                  ARGS: '-rltgoDzvO --delete'
                  SOURCE: 'deploy/'
                  REMOTE_HOST: 'project3.eaglesoftwareteam.com'
                  REMOTE_USER: ubuntu
                  TARGET: 'nodeapps/2022/project3/t6'
            - name: Execute SSH commmands on remote server
              uses: JimCronqvist/action-ssh@master
              env:
                  NAME: 'Root'
              with:
                  hosts: 'ubuntu@project3.eaglesoftwareteam.com'
                  privateKey: ${{ secrets.SERVER_SSH_KEY }}
                  debug: false
                  command: |
                      cd nodeapps/2022/project3/t6
                      npm install
                      sudo systemctl stop schedule-t6-backend
                      sudo cp schedule-t6-backend.service /lib/systemd/system/schedule-t6-backend.service
                      sudo systemctl daemon-reload
                      sudo systemctl start schedule-t6-backend